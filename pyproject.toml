[tool.poetry]
name = "pyXTEL"
version = "0.1.41"
description = ""
authors = ["vincenzo"]
packages = [
{ include = "*", from = "pyxsurf" },
]

[tool.poetry.dependencies]
python = "^3.7"

[tool.poetry.dev-dependencies]
pytest = "^4.6"

[build-system]
requires = ["poetry>=0.12"]
build-backend = "poetry.masonry.api"
